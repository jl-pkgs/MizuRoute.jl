#import "@preview/physica:0.9.8": dd, dv, pdv

=== Lohmann 单位线的推导与形式

Lohmann 单位线把流域视为线性、时不变系统：产流强度可以线性叠加，系统参数不随时间和流量改变。该方法将水从陆面到出口的运动分成“HRU 内坡面延迟”和“河网传播”两步 @lohmann1996。设 HRU $i$ 的径流深率为 $R_i(t)$、面积为 $A_i$，则其瞬时产流体积率为

$ q_i(t) = R_i(t) A_i. $

坡面响应并不限定为唯一函数。当前 mizuRoute 采用第 2.2 节所述 Gamma 单位线：

$ h_(s)(t) = 1/(Gamma(a) theta^a) t^(a-1) exp(-t/theta), quad t>0, $

其中 $a$ 为无量纲 shape 参数，$theta$ 为时间尺度。其均值、众数和标准差分别为

$ E[T_s] = a theta, quad T_("mode") = (a-1)theta quad (a>1), quad sigma_s = sqrt(a) theta. $

因此，$theta$ 主要控制整体延迟，$a$ 控制曲线形状及相对展宽；二者不能脱离时间步长和 HRU 尺度解释。VICTools.R 的 `routing_Lohmann.R` 不使用 Gamma 参数，而采用七日离散格内单位线

$ bold(h)_("box") = (0.15, 0.40, 0.25, 0.10, 0.06, 0.03, 0.01), $

也允许用户提供任意非负序列并归一化。两者作用相同，都是描述网格产流进入河道前的时间分配，但参数不可直接互换。

河道响应从线性一维扩散波方程出发：

$ pdv(Q, t) + C pdv(Q, x) = D pdv(Q, x, 2). $

在初始河道为空、上游于 $t=0$ 输入单位脉冲 $delta(t)$、下游距离为 $x$ 的条件下，该线性方程的单位脉冲响应可写为

$ h_(c)(x,t) = x/(2 t sqrt(pi D t)) exp(-(x-C t)^2/(4 D t)), quad t>0. $

这里 $C$ 为波速（m/s），控制平移时间；$D$ 为扩散系数（m²/s），控制削峰和展宽。理论响应对应逆高斯型旅行时间分布，其平均传播时间和方差为

$ E[T_c] = x/C, quad "Var"[T_c] = 2 D x/C^3. $

无量纲 Péclet 数

$ "Pe" = C x/D $

可衡量平移与扩散的相对强弱：$"Pe"$ 大时响应接近平移脉冲，$"Pe"$ 小时洪峰更低、历时更长。利用 Duhamel 原理，任意上游过程通过卷积得到下游流量：

$ Q(x,t) = integral_0^t Q_(in)(t-s) h_(c)(x,s) dif s. $

坡面与河道均为线性系统，故 HRU $i$ 到目标出口的总单位线是两者卷积：

$ h_i(t) = (h_(s) ast h_(c,i))(t), $
$ Q_("out")(t) = sum_i integral_0^t q_i(t-s) h_i(s) dif s. $

连续条件下 $integral_0^oo h(t) dif t=1$；离散后令 $sum_k w_k=1$，可保证单位径流体积仅在时间上重新分配，而不改变总水量。

*VICTools.R 中单位线的实际生成。* `routing_Lohmann.R` 按以下四步将连续公式变成可用于卷积的离散单位线。

（1）*生成单个河道单元的小时响应。* 令 $Delta tau=3600 "s"$，$t_m=m Delta tau$，$m=0,1,dots,48$。对路径中的河道单元 $j$，距离、波速和扩散系数分别为 $d_j,C_j,D_j$，先计算未归一化序列

$ tilde(u)_(j,0)=0, $
$ tilde(u)_(j,m)=d_j/(2 t_m sqrt(pi D_j t_m)) exp(-(C_j t_m-d_j)^2/(4 D_j t_m)), quad m=1,dots,48. $

再归一化为

$ w_(j,m)=tilde(u)_(j,m)/(sum_(r=0)^48 tilde(u)_(j,r)), quad sum_(m=0)^48 w_(j,m)=1. $

当 $d_j<2500 "m"$ 或距离缺失时，代码将其等效为离散单位脉冲 $bold(w)_j=(1,0,dots,0)$，即该单元不增加传播延迟。

（2）*生成源网格到出口的河道单位线。* 设源网格 $i$ 到目标出口依次经过 $p_i$ 个河道单元，路径为 $cal(P)_i=(j_1,dots,j_(p_i))$。初始化 $g_i^(0)=(1)$，沿 D8 路径递推

$ g_i^(r)=g_i^(r-1) ast bold(w)_(j_r), quad r=1,dots,p_i, $

其中离散卷积定义为

$ (a ast b)_m=sum_(ell=0)^m a_(m-ell)b_ell. $

因此，完整河道单位线为

$ bold(g)_i=bold(w)_(j_1) ast bold(w)_(j_2) ast dots ast bold(w)_(j_(p_i)), $

并再次令 $bold(g)_i slash sum_m g_(i,m)$ 归一化。该式就是 Lohmann source-to-sink 方法的核心：每个源网格拥有一条到指定出口的独立单位线。

（3）*截断、日尺度聚合并叠加坡面响应。* 代码保留前 $M=96 times 24=2304$ 个小时值，不足部分补零。第 $n$ 日的河道权重为

$ G_(i,n)=sum_(m=24(n-1))^(24n-1) g_(i,m), quad n=1,dots,96. $

令七日格内单位线的权重为 $b_ell$，则最终时间权重为

$ H_(i,n)=sum_(ell=1)^7 b_ell G_(i,n-ell+1), $

其中超出 $1 dots 96$ 的 $G_(i,n)$ 取零。这里 $bold(b)=(0.15,0.40,0.25,0.10,0.06,0.03,0.01)$；自定义 `uh_box` 会先除以自身总和。

（4）*加入面积和单位换算。* 设网格面积 $A_i$ 以 km² 计、流域内覆盖比例为 $f_i$，VIC 日径流 $R_(i,t)$ 以 mm/d 计，则代码保存的单位线系数为

$ U_(i,n)=H_(i,n) A_i f_i 1000/86400. $

其中 $1 "mm" times 1 "km²"=1000 "m³"$。最终出口流量为

$ Q_t=sum_i sum_(n=1)^96 R_(i,t-n+1) U_(i,n), $

单位为 m³/s。`Lohmann_conv` 中 $R_(i,t)$ 为 `OUT_RUNOFF + OUT_BASEFLOW`；矩阵乘法先汇总同一滞后日的全部上游网格，`aux_Lohmann_conv` 再沿时间方向完成上式卷积。

#table(
  columns: (0.65fr, 1.2fr, 1.2fr, 1.15fr, 2.1fr),
  [参数], [VICTools.R], [原版 mizuRoute], [本 Julia 实现], [应用含义与取值原则],
  [$a$], [不使用], [2.5], [2.5], [控制 Gamma 单位线形状；依据 HRU 出流过程率定],
  [$theta$], [不使用], [86400 s], [3600 s], [控制坡面平均延迟 $a theta$；需与 HRU 尺度和时间步匹配],
  [$C$], [1.5 m/s], [1.5 m/s], [1.0 m/s], [控制平均传播时间 $L/C$；依据洪峰到时或流速资料率定],
  [$D$], [800 m²/s], [5000 m²/s], [1000 m²/s], [控制河道响应展宽；依据洪峰衰减和过程线宽度率定],
)

这些数值是软件起始值，不是普适物理常数。以 $L=10 "km"$ 为例，VICTools.R 默认值给出 $L/C approx 1.85 "h"$、$"Pe"=18.75$；原版 mizuRoute 默认值给出相同平均传播时间和 $"Pe"=3$；本实现默认值给出 $L/C approx 2.78 "h"$、$"Pe"=10$。

*取值建议。*

+ *有河道水力资料时优先物理估算。* 根据代表性流量 $Q$、过水面积 $A$、水面宽度 $B$ 和河床坡降 $S_0$，取
  
  $ C=(pdv(Q, A))_(S_f), quad D=Q/(2 B S_0). $
  
  Manning 公式为
  
  $ Q=1/n A R_h^(2/3) sqrt(J). $
  
  代入 $D=Q/(2 B J)$ 得
  
  $ D=A/B R_h^(2/3)/(2n sqrt(J)), $
  
  其中 $R_h=A/P$ 为水力半径，$A/B$ 为水力深度。因此，一般断面仅有 $n,R_h,J$ 仍不足以唯一确定 $D$，还需断面面积 $A$ 与水面宽度 $B$。对宽矩形河道，$A/B approx R_h$，此时可直接估算
  
  $ D approx R_h^(5/3)/(2n sqrt(J)). $
  
  同时，宽矩形河道有 $Q prop A^(5/3)$，故可用 $C approx 5Q/(3A)=5v/3$ 作为初值，其中 $v=Q/A$ 为平均流速。

+ *有观测过程线时直接利用到时和展宽。* 设代表性传播距离为 $L$，扣除坡面延迟后的平均河道滞时和标准差分别为 $mu_T,sigma_T$，由逆高斯响应的矩关系取
  
  $ C=L/mu_T, quad D=sigma_T^2 C^3/(2L). $
  
  应先用洪峰到时或过程线质心率定 $C$，再固定 $C$，用洪峰衰减和过程线宽度率定 $D$。

+ *无资料时采用受约束的初始搜索。* 可先令 $C=0.5$--$3.0 "m/s"$，并用 $"Pe"=C L/D=3$--$20$ 约束 $D$，即
  
  $ C L/20 <= D <= C L/3. $
  
  例如 $L=10 "km"$、$C=1.5 "m/s"$ 时，$D$ 的初始范围约为 750--5000 m²/s；可从 $C=1.5 "m/s"$、$D=1000 "m²/s"$ 起算。该范围仅用于缺少资料时的敏感性分析，不应替代率定。

+ *检查离散时间窗。* VICTools.R 每个河道单元仅保留 48 h 响应，宜检查
  
  $ d/C + 3 sqrt(2 D d/C^3) < 48 "h". $
  
  若不满足，单位线尾部会被截断后重新归一化，应延长 `lcuh` 或将长单元分段；同时应检查源区到出口的主要响应小于总时窗 96 d。$d<2500 "m"$ 的单元被视为瞬时通过，调整 $C,D$ 对其无效。

#bibliography("refs.bib", title: "参考文献")
